def get_wind_dir(u : float, v : float) -> int :
    """Get wind direction in degrees from u and v component 

    Args:
        u (float): U component of wind
        v (float): v component of wind

    Returns:
        int: direction of wind in degrees
    """
    return None

def get_wind_speed(u : float, v:float) -> float : 
    """Get wind speed in knots from u and v component

    Args:
        u (float): u component of wind
        v (float): v component of wind

    Returns:
        float: wind speed in knots
    """
    return None